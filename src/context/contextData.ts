import { StoreDataI } from './interface';

const data: StoreDataI = {
  type: '',
  winH: window.innerHeight,
  winW: window.innerWidth,
  menuKeys: [],
  menuRefreshKey: 1,
  isMenuFold: false,
  leftMenuW: 200,
};

export default data;
